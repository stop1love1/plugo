#!/usr/bin/env python3
"""
Plugo CLI management commands.

Admin credentials are now configured via .env:
  USERNAME=admin
  PASSWORD=yourpassword

No database user management needed.
"""

print("Admin credentials are configured via .env (USERNAME / PASSWORD).")
print("No CLI user management needed.")
