# Development dependencies
# Install: pip install -r requirements-dev.txt

-r requirements.txt

# Linting & formatting
ruff>=0.6.0
mypy>=1.11.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
httpx>=0.27.0           # already in requirements.txt, needed for test client

# Pre-commit
pre-commit>=3.8.0
