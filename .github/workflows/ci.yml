name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-lint:
    name: Backend Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Ruff
        run: pip install ruff

      - name: Ruff lint
        run: ruff check backend/

      - name: Ruff format check
        run: ruff format --check backend/

  backend-build:
    name: Backend Build
    runs-on: ubuntu-latest
    needs: backend-lint
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('backend/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          cd backend
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Check syntax errors
        run: |
          cd backend
          python -m py_compile main.py
          python -m py_compile config.py

      - name: Run tests
        run: |
          cd backend
          python -m pytest tests/ -v

  dashboard:
    name: Dashboard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: dashboard/pnpm-lock.yaml

      - name: Install dependencies
        run: cd dashboard && pnpm install --frozen-lockfile

      - name: Lint
        run: cd dashboard && pnpm lint

      - name: Format check
        run: cd dashboard && pnpm format:check

      - name: Test
        run: cd dashboard && pnpm test

      - name: Build
        run: cd dashboard && pnpm build

  widget:
    name: Widget
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: widget/pnpm-lock.yaml

      - name: Install dependencies
        run: cd widget && pnpm install --frozen-lockfile

      - name: Lint
        run: cd widget && pnpm lint

      - name: Format check
        run: cd widget && pnpm format:check

      - name: Test
        run: cd widget && pnpm test

      - name: Build
        run: cd widget && pnpm build

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [backend-build, dashboard, widget]
    steps:
      - uses: actions/checkout@v4

      - name: Verify Docker Compose config
        run: docker compose config --quiet
